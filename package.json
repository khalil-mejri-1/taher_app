{
  "name": "mern-hostinger-deployment",
  "version": "1.0.0",
  "description": "Root package.json for Hostinger deployment",
  "scripts": {
    "build": "npm install --prefix client && npm install --prefix server && npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
